# Get default URLs for loading data.

`getURLs` get URLS to AWAP and Australian 9s DEM.

## Usage

``` r
getURLs()
```

## Value

A list variable of URLs as characters.

## Details

This function returns a list of default URLs used to download the AWAP
and DEM data.

## Examples

``` r
URLs = getURLs()
```
