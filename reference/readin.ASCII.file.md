# `readin.ASCII.file` reads in a grid data.

This is an internal function. It reads in an ARCMAP ASCII grid file.

## Usage

``` r
readin.ASCII.file(file.name, nRows, noData)
```

## Value

A matrix of the grid data.
