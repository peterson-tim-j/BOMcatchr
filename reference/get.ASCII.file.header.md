# `get.ASCII.file.header` get header information.

This is an internal function. It returns the header information within a
ARCMAP ASCII grid file.

## Usage

``` r
get.ASCII.file.header(
  data.type.label,
  workingFolder,
  datestring,
  remove.file = T
)
```

## Value

A list variable of header information.
